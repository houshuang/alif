# Test Backend

Run the full backend test suite and report results.

## Steps
1. Activate the Python venv: `source backend/.venv/bin/activate`
2. Run `cd backend && python -m pytest tests/ -v`
3. If tests fail, read the failing test and the relevant source file
4. Report: total tests, passed, failed, with details on failures
