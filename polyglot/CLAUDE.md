# Polyglot — Project Rules

This is the **second backend** in the `alif/` monorepo. It is *not* Alif. It serves Modern Greek (primary), Ancient Greek, and Latin via a separate process, database, and dependency set. Future Claude sessions: please read this file BEFORE editing anything under `polyglot/`.

## The two-backend distinction

| | `backend/` (Alif) | `polyglot/` (this) |
|---|---|---|
| Languages | Arabic (MSA) | Modern Greek, Ancient Greek, Latin |
| Primary use case | FSRS-driven sentence review | Reading-as-mapping (intermediate learner) |
| Port (dev) | 8000 | 3001 |
| Port (prod) | 3000 | 3002 (port 3001 is occupied by an unrelated Next.js service on the shared host) |
| DB file | `backend/alif.db` | `polyglot/polyglot.db` |
| systemd service | `alif-backend` | `polyglot-backend` |
| Python package | `alif-backend` | `polyglot-backend` |
| Heavy NLP | CAMeL Tools (Arabic morphology) | simplemma + GR-NLP-TOOLKIT + Claude verifier |
| Frontend tab | "Reading" / "Stories" / "Listening" etc. | "Reading" (polyglot.tsx) |

**Same monorepo, totally separate code.** `backend/` does not import from `polyglot/` and vice versa. The frontend (`frontend/`) talks to *both* — the active backend is chosen by the user's language selection (see `frontend/lib/language-context.tsx`).

## Ground design and code in Alif

**Alif is 100+ days of iteration and daily real-user testing.** Every UI affordance, scheduling constant, gate, review semantic, payload shape, button label, and empty-state copy line in Alif has a history — a bug, a confusion, a feature request, a thing that worked after several that didn't. Polyglot is not a fresh design exercise. It is a port. **Mirror Alif by default. Divergence requires a specific Greek/Latin-driven reason, recorded in the change itself (PR body, CLAUDE.md note, or experiment log).**

Concretely, this applies to:

- **UI / UX.** Before designing or porting any screen, read Alif's equivalent file (the actual TSX, not just the docs). Sentence card layout, two-stage reveal, per-word tap semantics, action-row positioning, button labels (including the toggling label trick — see Alif's "Know All" → "Continue" middle button), tashkeel-toggle analogues, empty-state copy, session-end framing. Do not invent a "cleaner" rating model, a "simpler" partial flow, or a "more modern" gesture. Real users have already filtered Alif's choices.
- **Scheduling / SRS values.** FSRS desired-retention, Leitner intervals (4h / 1d / 3d), daily intro cap (30), tier graduation thresholds, leech detection window (8 reviews, <50%), comprehension-aware cooldowns (7d / 2d / 4h) — copy verbatim. Polyglot doesn't have its own data yet to justify a refit.
- **Data model + API shape.** Field names, payload structure, idempotency keys (`client_review_id`), enum string values (`understood` / `partial` / `no_idea`, not `understood` / `partial` / `none`). Phase-2 `alif_core` extraction will be vastly easier if both backends already speak the same dialect.
- **Code structure.** Where reasonable, mirror file layout, function names, and helper boundaries. `submit_sentence_review`, `start_acquisition`, `_intro_shown_recently` — same names. Future diffing into shared code expects this.

**Specific, defensible reasons to diverge — language-driven, not preference-driven:**

- Arabic-specific machinery doesn't apply: clitic stripping, awzān, tashkeel toggle, Hindawi-tier seed selection, CAMeL Tools morphology, root-pattern reasoning, mappings against an Arabic frequency core. **Cut, don't port.**
- Greek/Latin-specific needs that Alif doesn't have: simplemma lemmatization, accent restoration on all-caps headings, Modern↔Ancient cognate auto-linking, L1 cognate detection (φιλοσοφία → philosophy). **Add, don't try to retrofit into Alif's shape.**
- Endpoints polyglot has intentionally not built yet (no TTS → no `audio_play_count`, no confusion-help service → no `confusion_candidate_lemma_ids`, no intro cards → no `experiment_intro_shown_at`). When porting an Alif client call, **drop the fields the polyglot backend doesn't accept** rather than inventing a stub.

**Before introducing a feature or making a design choice without an Alif analogue**: search Alif first. If Alif doesn't have it, ask which case applies:
1. Alif doesn't need it (e.g. cognate auto-linking — Arabic has fewer transparent cognates). Adding it in polyglot is fine.
2. Alif hasn't implemented it yet but plans to (suspect). Mirror Alif's absence — don't preempt Alif's eventual design, because when Alif does add it, you'll either have to retrofit polyglot to match or fork the design permanently. Neither is what we want.

The Phase-2 `alif_core` extraction (see bottom of this file) is the eventual payoff for this discipline. The more divergent polyglot becomes from Alif, the smaller the shared surface, and the more we end up maintaining two divergent systems forever.

## When working in polyglot/

- **Always run `polyglot/.venv/bin/python`**, not `backend/.venv/bin/python`. The venvs have incompatible binary deps (Arabic uses arm64 torch, polyglot has its own setup).
- **Code-port discipline (complements the design rule above).** When porting an Alif service, read it before you write the polyglot version, then strip Arabic-specific assumptions (clitic stripping, awzān, tashkeel, Hindawi corpus) and keep the rest. Don't copy verbatim — port deliberately, mindful of Phase-2 `alif_core` extraction — but don't reinvent either.
- **Don't apply Arabic invariants to Greek.** The "no auto-create lemmas" rule from Alif's CLAUDE.md was tuned for the bookCorpus + LLM-generated-sentence pipeline. In polyglot, the LLM quality gate is *allowed* to create new Lemma rows (with `source='quality_gate'`) because the source of truth is an authentic imported text and the correct lemma must be representable. See `app/services/lemma_quality.py` — there's a comment block making this explicit.
- **Use `simplemma` for lemmatization, not GR-NLP-TOOLKIT.** GR-NLP-TOOLKIT 0.3.0 does not include a lemmatizer — only POS, NER, morphology, dependency parsing. We learned this the hard way (see commit history). For Modern Greek lemmas: simplemma. For richer POS/morph (when needed): GR-NLP-TOOLKIT.
- **The HuggingFace cache is project-local.** `app/services/languages/el.py` sets `HF_HOME` to `polyglot/data/hf_cache/` *at module import time* — must run before any transformers/torch import. Don't move it out of that file or you'll re-trigger the sandbox-write permission error.
- **New frontend screens must live at `app/polyglot-*.tsx`** (or under `app/polyglot/`). That URL prefix is what makes `frontend/app/_layout.tsx` isolate Greek screens from the Arabic tab bar via `routeLanguage()` in `frontend/lib/language-routes.ts`. A non-prefixed filename will silently be classified as Arabic and fail the `language-context.test.ts` manifest check. See *App shell & language routing* in `docs/frontend-files.md` for the full rule.

## Hard invariants (polyglot-specific)

These should be preserved across changes:

1. **Lazy page processing.** Pages are tokenized + LLM-verified only when first viewed (`GET /api/texts/{sid}/pages/{n}`). Importing a 300-page textbook must be fast — the cost is paid per-page-view, not upfront. Implemented in `app/services/reading_intake.py:process_page`.

   Four LLM passes happen per page, in order: (a) **body-clean** (`body_clean.py`) — strips page numbers, headers, footers, bibliographies, footnote definitions; joins soft-hyphen line breaks; detaches footnote-marker digits fused to words. Cleaned prose lands in `page.body_clean` and the tokenizer reads from there. (b) **batch-gloss** (`lemma_gloss.ensure_glosses_batch`) — fetches an English gloss for every content lemma on the page in chunks of `POLYGLOT_GLOSS_CHUNK=50`, caches in `Lemma.gloss_en`. Function words and proper names are filtered before the call. (c) **citation repair** (`lemma_integrity.repair_lemmas`) — audits every newly-created lemma's stored form + gloss and renames/merges/retire-noises it before study state can attach. (d) **quality gate** (`lemma_quality.py`) — per-token lemma verification. All structured calls go through `app/services/llm_cli.py`; production uses Codex headless (`POLYGLOT_LLM_PROVIDER=codex`, `POLYGLOT_CODEX_MODEL=gpt-5.5`, `CODEX_HOME=/opt/alif/.codex`), while Claude remains a supported fallback provider. The body-clean pass runs first because it dictates what the tokenizer sees; sending raw PyMuPDF text to the tokenizer creates phantom lemmas (`σιτηρών1`, `μη`, `χάνημα`, etc.) the user will never want to learn. Gated by `POLYGLOT_BODY_CLEAN=1` / `POLYGLOT_BATCH_GLOSS=1` (both default on); body-clean falls back to raw `body_src` on LLM failure, batch-gloss leaves NULL gloss for any failed chunk and the per-word `ensure_gloss` covers the cache miss when the user actually taps.

2. **Citation repair + quality gate are the lemmatization safety net.** simplemma misclassifies inflections, homographs (χώρα → χωρώ), proper nouns (Τίγρης → τίγρη), POS confusions (adj↔noun), and σε-crasis forms. `lemma_integrity.py` fixes the Lemma row itself; `lemma_quality.py` fixes each PageWord mapping in sentence context. Enabled by `POLYGLOT_LEMMA_REPAIR=1` and `POLYGLOT_QUALITY_GATE=1`. **Do not skip this for "speed" — Alif spent months retroactively fixing bad mappings; we don't redo that mistake here.**

3. **Bulk-mark and review scheduling presume content lemmas only.** `bulk_mark_remaining_known()` skips lemmas where `word_category='function_word'` OR `lemma_bare` is in `FUNCTION_WORD_SETS`; sentence review and the review scheduler also skip `proper_name` and `not_word`. These lemmas can remain mapped in sentence text, but they must not become review targets, intro cards, collateral credits, or active due rows. `lemma_quality.is_noncontent_lemma()` is the shared predicate; update it when adding another non-content class. The reader must also treat a bare-form function word as non-content even if an old row lacks `word_category`; `reading_intake._build_token_view` does this so legacy rows don't appear as new vocabulary. Quality-gate corrections and page intake stamp `word_category='function_word'` when the corrected/existing lemma bare form is in `FUNCTION_WORD_SETS`. Heading sentences (≥80% all-caps tokens, ≤10 words) are marked `quality_note='heading'` by the quality gate and should be excluded from review eligibility — they're meta-text, not vocabulary. Harvested textbook review sentences also exclude page-boundary fragments: if a PDF page ends mid-sentence or the previous page ended mid-sentence, `sentence_harvest.py` omits the cut first/last sentence from reusable `Sentence` rows while leaving `PageWord` rows intact for the reader. When repairing imported pages, `scripts/backfill_body_clean.py --reprocess` must update existing harvested sentences in place and deactivate stale fragments, never delete `Sentence` ids, because `review_log` and `sentence_review_log` hold historical FKs. **Production backfill 2026-05-23:** legacy `λοιπόν` and `παρά` rows were marked `function_word`; ungated content lemmas returned to 0.

4. **Cognate propagation is 'encountered', not 'known'.** When user marks Modern φιλία as known, the Ancient cognate (via `cognate_lemma_id`) becomes `encountered` (NOT `known`). Semantic drift between Modern↔Ancient Greek is real (Modern άλογο "horse" ↔ Ancient ἄλογος "irrational"). Auto-promoting would create silent errors.

5. **Modern↔Ancient bare-form auto-linking is bidirectional and idempotent.** `link_intra_greek_cognates()` runs on every Lemma creation. It's cheap (DB lookup), no LLM call.

6. **External L1 cognate detection is opt-in, and `low` is not actually usable.** Gated by `POLYGLOT_DETECT_COGNATES=1` and `POLYGLOT_AUTO_MARK_COGNATES=1`; bulk-import scripts can pass `auto_mark=True` / `batch_size=N` directly to `detect_external_cognates` to override the env defaults. `UserProfile.cognate_auto_mark_threshold` accepts `high` / `medium` / `low` / `never`. **Use `medium` in practice** — see the 2026-05-21 SUBTLEX-GR seed run below. The detector's `--json-schema` MUST be `type: "object"` at the top level (Anthropic API constraint); regression covered in `tests/test_cognates.py::test_external_cognate_parser_rejects_bare_array`.

   **2026-05-21 SUBTLEX-GR seed lesson — `low` tier is form-blind.** The initial run (`scripts/import_subtlex_gr.py --top 5000 --threshold low`) auto-marked 2,525 ULKs across high (37%) / medium (26%) / low (37%). Manual audit found the `low` tier had ~50% etymology-only false positives (πικρός→picric, παραλείπω→paralipsis, αλεπού→alopecia). `scripts/recheck_low_cognates.py` re-judged the 939 low-best marks with a tighter "would a reader recognize this at sight" prompt and dropped 681 (72%), leaving 258 low-tier "survivors." A second-look audit on the survivors revealed the recheck shared the same blind spot as the original detector: **both LLMs accept "etymological link + matching gloss" as recognizable, without ever checking that the Greek surface form actually transliterates to anything like the proposed cognate.** πτυχίο→diploma "survived" the recheck despite having zero shared letters (different Greek roots — πτυχή vs δίπλωμα — same English gloss). All 258 low survivors were dropped 2026-05-21 (see `cognate_lowtier_dropped` ActivityLog entry). Final cognate pool: **1,586 ULKs from high + medium tiers only**. The correct fix is a transliteration-distance filter (deterministic, no LLM judgment); not built yet — track it in IDEAS if you ever raise the threshold back below `medium`.

   Cognate-known and bulk-pre-known rows are **assumed-known scaffolding**, not retrieval targets. They should enrich the known-word pool and candidate comprehensibility, but `warm_sentence_cache` must not generate dedicated review material for them unless the learner later misses the word and it enters acquisition, or it has an actual FSRS card. This was tightened 2026-05-23 after a coverage audit incorrectly counted ~1,968 active content lemmas instead of the real ~146 retrieval targets.

7. **All structured LLM calls go through `llm_cli.py`.** Do not hand-roll provider-specific subprocess calls in individual services. The wrapper preserves the Claude envelope parser (`structured_output` first, legacy `result` fallback) and converts service schemas into Codex/OpenAI strict schemas (`additionalProperties:false`, every property required, formerly optional fields nullable). Production uses Codex headless; token material lives in `/opt/alif/.codex` and `/opt/alif/.env` and must never be printed.

8. **Verification failure ≠ success.** If an LLM verifier call fails (timeout, parse error, non-zero CLI exit), the function returns `None` and tokens/sentences stay unverified. We never silently treat an LLM failure as "all good." When *any* batch on a page returns `None`, `mappings_verified_at` is left NULL so the next page-view retries. Per-word `verified_at` is still set for tokens whose batch succeeded, so retries only re-send the failed batches.

9. **Canonical lemma is the unit of scheduling.** Variant lemmas (`Lemma.canonical_lemma_id NOT NULL`) must never get their own `UserLemmaKnowledge` row. Every code path that creates a ULK redirects through `canonical_resolution.resolve_canonical_lemma_id(db, lemma_id)` at entry. Currently the redirect lives in `start_acquisition`, `submit_review` (FSRS), `submit_acquisition_review`, `mark_lemma` (every state branch), `propagate_known_via_cognate`, `_auto_mark_known`, the `/api/reviews/submit` + `/api/reviews/introduce` router handlers, and `sentence_review_service.submit_sentence_review` (via `resolve_canonical_via_map` on a pre-loaded sentence-lemma map — hot-path pattern). **When adding a new ULK-creation path, redirect at function entry — don't trust callers.** Regression coverage in `tests/test_canonical_resolution.py` and `tests/test_sentence_review_service.py::test_variant_credit_goes_to_canonical` asserts variant-in → canonical-out on every entry point.

10. **Mark-unknown enters the SRS engine and bypasses the daily intro cap.** `reading_intake.mark_lemma(state='unknown')` calls `start_acquisition(due_immediately=True, source='reading_intake')`, so a tapped unknown word lands in Leitner Box 1 with the first review due now. `reading_intake` is in `acquisition_service.CAP_EXEMPT_SOURCES` alongside `leech_reintro`: a user's "I don't know this" tap is data the system must always honour (the data-capture logic) — separate from how the scheduler then paces practice (the scheduling logic). Recovery thresholds are also looser than Alif's (`RECOVERY_BOX1_UNREVIEWED_LIMIT=50`, `RECOVERY_MIN_REVIEWS_FOR_ANY_INTRO=5`) so other sources don't lock out on day 1. Diverges from Alif Hard Invariant FOUNDATIONAL where the cap binds inside `start_acquisition` itself; in polyglot the dominant intro source is the reading screen, where the user expresses intent explicitly. Don't reintroduce a standalone `unknown` state in this flow — the whole point of the port is that unknown-flagged words actually flow into review. **Production incident 2026-05-20**: prior to this exemption, 23 of the first 28 red taps on Greek textbook page 1 silently downgraded to `encountered` because `RECOVERY_BOX1_UNREVIEWED_LIMIT=5` + `RECOVERY_MIN_REVIEWS_FOR_ANY_INTRO=20` is unreachable on day 1.

11. **Review log idempotency via `client_review_id`.** When the frontend retries a submission after offline reconciliation, the second call must observe the post-state from the first, not apply a second FSRS step. The unique index on `ReviewLog.client_review_id` enforces this at the DB level; the service code reads it first and returns `duplicate=True`.

12. **Intro-card working-memory gate is ported.** `UserLemmaKnowledge.experiment_intro_shown_at` is stamped by `POST /api/reviews/experiment-intro-ack` when the frontend displays an intro card. `_intro_shown_recently(ulk, now)` returns True while the gap is below `FAST_GRAD_INTRO_GAP=10min`, in which case `submit_acquisition_review` blocks Tier 0 / Tier 1 / Tier 2 fast-graduation paths AND the Box 1→2 advancement — three correct answers seconds after an intro is working memory, not learning. Correct reviews inside the window count for exposure but keep the word in Box 1 on `FAST_INTRO_RETRY_INTERVAL=30min` instead. The same field doubles as (a) the dedup key for `_build_intro_cards` (don't re-emit), and (b) the rescue-cooldown timestamp (≥4 reviews <50% accuracy → re-teach card, with a `RESCUE_COOLDOWN_DAYS=7` cooldown). Intro cards are emitted by `sentence_selector.build_session` for never-shown acquiring lemmas appearing in the picked sentences; the frontend interleaves them before the target sentence via `buildInterleavedSlots`. Suspended for lemmas with no ULK (no fabricated state).

13. **Collateral sentence exposure is not explicit acquisition.** `sentence_review_service` may auto-promote a new/encountered content lemma from a sentence as `source='collateral'`, but same-sentence correctness must not graduate it or advance Box 1 before its `acquisition_next_due`. Collateral words need spaced confirmation: first exposure counts toward `times_seen`/accuracy and keeps the existing due time; later due reviews can advance boxes, and Tier 3 remains the normal graduation path. This prevents "I understood the sentence" from turning incidental words into FSRS cards after one exposure.

## Gates audit — ported from Alif vs deferred

| Alif gate | Polyglot status | File / notes |
|---|---|---|
| PDF body cleanup (page numbers, headers, footnotes, bibliographies, soft-hyphens) | **Ported (polyglot-original)** | `app/services/body_clean.py` — Haiku via CLI, verbatim-audit on removed segments (soft-hyphen + footnote-digit + control-char tolerant), persisted as `Page.body_clean`. Alif doesn't have this because Alif imports from cleaned corpora rather than raw PDFs. |
| Lemma quality verification (sentence-context LLM check) | **Ported** | `app/services/lemma_quality.py` |
| Mapping correction pipeline (`apply_corrections`) | **Ported** | `_apply_verdict()` in lemma_quality.py |
| Verification failure ≠ success | **Ported** | `_call_claude` returns `None` on failure |
| Non-content lemma exclusion | **Ported** | `lemma_quality.is_noncontent_lemma` centralizes language function-word bare forms plus `word_category in {"function_word","proper_name","not_word"}`. Used by bulk-mark, sentence review, due endpoints, session builder, picker target guard, acquisition stats, and cleanup script. |
| Canonical lemma chain (`canonical_lemma_id`) | **Ported** | `canonical_resolution.py` exposes `resolve_canonical_lemma_id` (DB-backed, multi-hop, cycle-safe) and `resolve_canonical_via_map` (pre-loaded map, hot path). Redirect at function entry on all 7 ULK-creation sites: `start_acquisition`, `submit_review`, `submit_acquisition_review`, `mark_lemma` (any state), `propagate_known_via_cognate`, `_auto_mark_known`, plus the reviews router for response payloads. `sentence_review_service.submit_sentence_review` resolves via `resolve_canonical_via_map` against a pre-loaded sentence-lemma map (hot-path pattern; sentence_harvest already canonicalised at storage, so this is defense-in-depth for imports that bypassed it). |
| Cross-language cognate links (`cognate_lemma_id`) | **Ported + extended** | Unique to polyglot — Alif doesn't have this concept |
| External L1 cognates | **Ported + bulk-seeded (high+medium only)** | `cognate_detector.py` (env-gated for incremental reading-intake); `scripts/import_subtlex_gr.py` ran 2026-05-21 against top-5000 SUBTLEX-GR lemmas at threshold=`low`, then `scripts/recheck_low_cognates.py` ran, then all 258 surviving low-tier marks were dropped (form-recognizability gap — see Hard Invariant 6). Production pool: 1,586 cognate ULKs. **Use threshold=`medium` for any future bulk runs until a transliteration filter is added.** |
| Gloss-on-demand (single-lemma fallback) | **Ported** | `lemma_gloss.ensure_gloss` — called from `mark_lemma(state='unknown')` when the batch cache missed. ~3s per word; surfaces a "…" placeholder during the round-trip. |
| Batch gloss cache (every lemma on page) | **Ported (polyglot-original)** | `lemma_gloss.ensure_glosses_batch` — chunked Haiku call at `POLYGLOT_GLOSS_CHUNK=50` per call, filters function words + proper names before the LLM, per-chunk commit (no SQLite write lock across calls). Wired into `process_page` Phase 2b. Alif doesn't have this because Alif's lemmas already arrive with glosses via the corpus pipeline. |
| `--json-schema` for constrained CLI decoding | **Ported** | Uses `structured_output` field |
| Two-phase commit (NLP work outside DB transaction) | **Ported** | `process_page` does compute-then-write |
| No bare word cards (sentences only) | **Ported** | Read- and write-side both wired and now served end-to-end. Read-side: `sentence_selector.pick_sentence_for_lemma` + `sentence_selector.build_session` (PR #3) → `GET /api/reviews/next-sentence` and `GET /api/reviews/session`. Write-side: `sentence_review_service.submit_sentence_review` + `POST /api/reviews/submit-sentence` (PR #2). Frontend: `frontend/app/polyglot-review.tsx` (PR #5) is now a sentence card — two-stage reveal, per-word tap cycling off → missed (red) → confused (yellow), 3-signal comprehension model, middle button label toggles "Know All"↔"Continue" based on marks. Mirrors Alif's `SentenceReadingCard` + `ReadingActions` design verbatim, with Arabic-specific bits cut (tashkeel toggle, transliteration, lookup panel, confusion analysis, intro cards, audio). The bare-word `POST /api/reviews/submit` endpoint remains for ad-hoc lookups but is no longer the primary review surface. **2026-05-21 click-state alignment** (PR #118): re-grounded the sentence render in Alif — no word carries a pre-applied highlight (dropped `is_target` italic + currently-glossed-word italic + function-word style); any word with a `lemma_id` cycles visually, and `lemmaIdsFromMarks` keeps stripping function words / proper names from the submit payload so FSRS credit stays content-only; the gloss / missed-word card is now driven entirely by the last red/yellow word (close-via-cycle, no standalone close button). **Session restore across word-detail (2026-05-22):** because every screen is a flat sibling in one `<Tabs>` navigator (no `<Stack>`), opening `polyglot-lemma/[id]` from a review card (intro "View full philology" or the tapped-word gloss card) tears this screen down; on return the mount would `loadSession()` a *fresh* server-generated session and drop the learner's place — sentence, reveal state, and word marks all lost. Fixed by snapshotting the whole in-flight session (`bundle` + interleaved `slots` + `index` + `cardState` + `marks` + `glossWordIdx` + `sessionId` + shown-intro set) to AsyncStorage (`@polyglot:reviewSnapshot`, 15-min recency gate) and rehydrating it on mount instead of reloading; `marks` is flattened to arrays since `Set` isn't JSON-serializable. Written on every session-state change and again right before `router.push` into the detail screen; cleared in `loadSession` when a fresh session comes back empty, **and on final-slot advance** (before the next-session load) so a failed/interrupted reload can't leave the snapshot pointing at a just-submitted card. Restore is submit-safe: reviews only fire on button press with a fresh `client_review_id`, and the restored `index` sits past already-submitted slots; the final-slot clear closes the one window where that wasn't true (a re-submit there would mint a new `client_review_id` and bypass backend idempotency). Mirrors Alif's AsyncStorage session-cache approach (`index.tsx`), not a `<Stack>` migration. **Alif-mirror polish (2026-05-22, PR #131):** dropped the "Hide Translation" button — reveal is one-way like Alif (back side = `[No idea] [Know All/Continue]` + empty spacer); action row restyled to Alif's filled buttons (moss "Know All" → burnt-orange "Continue", burnt-orange "Show Translation", 14px radius) and **pinned to the bottom** of the screen (flex `container` → `ScrollView(flex)` → `bottomActions`, was floating after scroll content); the tapped-word gloss card now **stays open across the front→back flip** (`advanceCard` no longer clears `glossWordIdx`); the progress header shows only "Card X of Y" — the internal `selection_reason` (`best_available`, etc.) no longer leaks into the UI. **Overflow ("...") menu (2026-05-23):** sentence cards now use the shared Alif `ActionMenu` with polyglot-specific clients. Actions: **Ask AI** (`POST /api/chat/ask`, CLI-backed tutor with the current sentence, translation, word marks, and lookup focus in context), **Report sentence** (`POST /api/flags`, `content_type="sentence"`, deduped against pending/reviewing reports), **Refresh session** (clears the snapshot and `loadSession`s a fresh server session), and **Back** (`router.canGoBack()` → back, else `/polyglot`). Intro cards still use the lean Folio `OverflowMenu` with Refresh/Back only because there is no sentence to explain or report. Suspend stays hidden on polyglot review cards. |
| Sentence review (per-word credit, collateral semantics) | **Ported** | `sentence_review_service.py` — distributes one comprehension signal across every content lemma in the sentence (target + collateral), honouring Hard Invariant FOUNDATIONAL. Non-content lemmas (`function_word`, `proper_name`, `not_word`, or language function-word bare forms) are skipped via `is_noncontent_lemma`. Variant-in → canonical-out at function entry. Cap-deferred encountered words bump `total_encounters` without a review. New/encountered collateral lemmas auto-promote to acquisition but cannot fast-graduate or advance before due. New tables: `sentence_review_log`; new ReviewLog columns: `credit_type`, `was_confused`. Includes `undo_sentence_review` that restores pre-state from `fsrs_log_json` snapshots. |
| FSRS scheduling | **Ported** | `fsrs_service.py` — py-fsrs v6, desired_retention=0.95 (Alif's optimizer fit; refit when polyglot has its own data). Mnemonic regeneration deferred. |
| Acquisition Leitner 3-box | **Ported** | `acquisition_service.py` — 4h/1d/3d intervals + tiered graduation (Tier 0 first-correct, Tier 1 100%, Tier 2 ≥80%, Tier 3 standard). Fast graduation requires a due, non-collateral card. Intro-card working-memory gate **ported** — `_intro_shown_recently` blocks Tier 0/1/2 + Box 1→2 advancement within `FAST_GRAD_INTRO_GAP=10min` of `experiment_intro_shown_at`; correct reviews inside the window count for exposure but reschedule via `FAST_INTRO_RETRY_INTERVAL=30min`. Early same-session collateral reviews count exposure but preserve Box 1 and the existing due time. |
| Session builder | **Ported** | `sentence_selector.build_session` walks acquisition-due (Box 1→2→3) then FSRS-due, filters non-content due rows, picks one sentence per lemma via the picker, dedupes within session, respects `limit`, and returns skipped-due diagnostics for logging. Intro/rescue cards are emitted for eligible content lemmas in the selected sentences. |
| Intro card filter | **Ported** | `sentence_selector._build_intro_cards` emits "new" cards for acquiring lemmas appearing in the session that have never been reviewed (`times_seen=0`, `experiment_intro_shown_at IS NULL`) and "rescue" cards for stuck lemmas (≥`RESCUE_MIN_SEEN=4` reviews, <`RESCUE_MAX_ACCURACY=50%`, last-shown >`RESCUE_COOLDOWN_DAYS=7` ago). Non-content lemmas skipped. Dynamic cap scales with un-introed backlog (base 4, +1 per 15, max 6); `INTRO_NEW_CARDS_PER_SESSION=6` is the total session budget. The frontend (`frontend/lib/polyglot-review-helpers.ts:buildInterleavedSlots`) interleaves them before the target sentence and posts `/api/reviews/experiment-intro-ack` on display. |
| Comprehensibility gate | **Ported (picker-side)** | `sentence_selector._score_candidate` weights candidates by fraction-of-scaffold-known and applies a strong page-first bonus only when ALL content scaffold lemmas are in `known`/`learning` state. Generation-side prompt asks Sonnet to prefer scaffold-rich sentences but doesn't enforce a strict ratio — the read-side picker has the final say. |
| Material generation (LLM sentences for due lemmas) | **Ported** | `material_generator.py` — `batch_generate_material(language_code, lemma_ids)` runs one generation call + one verification call per batch through `llm_cli.py` (Claude aliases still accepted; production maps them to Codex via `POLYGLOT_LLM_PROVIDER=codex`). Three-phase read/LLM/write pattern; never holds the SQLite write lock across LLM calls. Mandatory verification gate (Hard Invariant): on total verifier failure the entire batch is discarded; per-sentence "wrong" verdicts drop that candidate. Glossless target check at function entry. Canonical resolution via `resolve_canonical_via_map` at write time. Function-word tokens may have `lemma_id=NULL` (matches `sentence_harvest`). Endpoint: `POST /api/materials/generate`. **2026-05-21 quality pass**: `KNOWN_SAMPLE_SIZE` bumped 60→500 (matches Alif; engaged pool is ~2.4k lemmas so the LLM has plenty to scaffold from). New inverse-frequency weighted sampling (`_sample_known_words_weighted`) + avoid-words list (`_compute_avoid_words`) for over-represented vocab. Prompt strengthened: explicit "every non-target word MUST come from this pool" rule. simplemma `greedy=True` for ~3% more inflected-form recall. **2026-05-23 prompt/yield pass:** prompt now rejects heading/list fragments, requires ordinary complete sentences with finite verbs, asks Modern Greek generation to prefer forms the dictionary-backed mapper can resolve, and warns against clipped article-less textbook-heading style. |
| Warm sentence cache | **Ported** | `material_generator.warm_sentence_cache(language_code, max_lemmas)` finds **retrieval targets**, not every assumed-known lemma: all `acquiring` rows plus `learning`/`known`/`lapsed` rows that have an FSRS card. Cognate-known / bulk-pre-known rows with no FSRS card are scaffold vocabulary only and do not get dedicated generated review material unless later missed. Coverage counts only active, verified, quality-approved `source="llm"` sentences toward `POLYGLOT_ACTIVE_TARGET=3`; harvested textbook sentences remain picker fallbacks and do not satisfy the generated-material target. Acquiring lemmas sorted by `acquisition_next_due ASC` so the cache prioritises what the next session will pull. Variant / function-word / proper-name / not-word / glossless lemmas filtered out at the gap query; language function-word bare forms are filtered via `is_noncontent_lemma`. Threading lock prevents concurrent runs from double-spending LLM budget on the same gaps. Endpoint: `POST /api/materials/warm-cache`. CLI: `scripts/warm_sentence_cache.py`. Cron wrapper: `deploy/polyglot-update-material.sh`. Production cron runs every 3h at 48 lemmas/run by default and, as of 2026-05-23, also runs `scripts/translate_sentences.py` after generation. |
| Book-sentence translation | **Polyglot-original (2026-05-22)** | `material_generator.translate_untranslated_sentences(language_code, max_sentences)` — harvested textbook sentences are created with `translation_en=NULL` (harvest holds no LLM call), so a book sentence served as a picker fallback would render with a blank English line. This pass fills `translation_en` for untranslated active+verified textbook sentences that cover an active-study lemma (the only book sentences the picker can serve), newest first, through `llm_cli.py` (`POLYGLOT_TRANSLATE_MODEL`, batched at `POLYGLOT_TRANSLATE_BATCH_SIZE=12`). Three-phase read/LLM/write; idempotent (only fills still-NULL rows); threading lock. **Lazy: runs only in the cron's pre-warm window, never on the read path** (per user 2026-05-22). Endpoint: `POST /api/materials/translate-sentences`. CLI: `scripts/translate_sentences.py`. Cron phase 3 in `deploy/polyglot-update-material.sh`. Alif's analogue is corpus on-demand translation via its own cron step. |
| Variant chain resolution everywhere | **Ported** | `canonical_resolution.py` — multi-hop resolver with cycle protection. `start_acquisition` and the reviews router redirect at function entry. |
| Proper-name / junk handling | **Ported** | `Lemma.word_category='proper_name'` and `word_category='not_word'` are non-content. They can be mapped for text display and quality gates, but are not schedulable targets or collateral review credits. |
| Leech auto-management | **Ported** | `leech_service.py` — sliding-window detection (last 8 reviews <50% accuracy), graduated cooldowns (3d/7d/14d), preserved stats across cycles, low-priority multiplier on rare lemmas (rank > 5000). |
| Listening readiness gate | N/A (no TTS yet) | |
| Audio cache by SHA256 | N/A | |
| Daily intro cap | **Ported** | `DAILY_INTRO_CAP=30` net-new acquisitions per UTC day, enforced inside `start_acquisition`. Recovery-mode budget kicks in under Box 1/2 overload (same thresholds as Alif). `leech_reintro` bypasses the cap. |
| Review log idempotency | **Ported** | `ReviewLog.client_review_id` is unique + indexed; submit returns `duplicate=True` for replay. Mirrors Alif's offline-queue contract. |
| Interaction log JSONL | **Ported** | `interaction_logger.py` — daily files at `polyglot/data/logs/interactions_YYYY-MM-DD.jsonl`. Disabled only for truthy testing values (`TESTING=1/true/yes/on`); `TESTING=0` logs normally. Review routes log `next_sentence_selected` and `session_built` with candidate counts, selected sentence IDs, target lemmas, reasons, times-shown, recency, intro IDs, and skipped-due diagnostics. `sentence_review_service` logs `sentence_review` with comprehension signal, missed/confused IDs, word results, duplicate status, response time, and leech suspensions. |
| Activity log (ActivityLog table) | **Ported** | `activity_log.py` — service-level events (batch jobs, leech sweeps). Used by leech_service. |
| Lemma philology enrichment | **Polyglot-original (2026-05-21)** | `lemma_philology.py` — `batch_enrich(language_code, lemma_ids)` produces `LemmaEnrichment` payloads (etymology + diachrony + cognates + literary quotes + register/collocations) for engaged-vocabulary lemmas. Generation + fact-verify (etymology + diachrony only — quotes/cognates/register skipped per design) + **inline self-correct** when the verifier flags. Three-phase read/LLM/write via `llm_cli.py`. **Status taxonomy on `Lemma.enrichment_status`** (post-self-correct, 2026-05-21): `done` (generation + verifier OK, possibly after one or two self-correct rounds), `done_unverified` (verifier itself failed to run, generated payload kept), `done_partial` (verifier persistently flagged after `SELF_CORRECT_MAX_ATTEMPTS=2` rounds — the flagged top-level field is **stripped** before write so a wrong PIE root / wrong diachrony stage never reaches the lookup card; `_verifier_note` records `verdict` + `stripped: True`), `failed` (generation never produced a parseable payload). **Legacy `done_flagged`** (pre-self-correct) is no longer written; existing prod rows get drained by the cron's `--include-failed` flag. Three new Lemma columns: `enrichment_json` (JSON), `enrichment_status` (String), `enriched_at` (DateTime). Endpoints: `POST /api/materials/enrich-philology`, `GET /api/lemmas/{id}/detail`. CLI: `scripts/enrich_lemma_philology.py`. Cron phase 4 in `deploy/polyglot-update-material.sh` (runs with `--include-failed` so `failed` / `done_flagged` / `done_partial` rows are retried on every pass — they never linger). **Single source of truth for the payload shape**: `app/schemas.py::LemmaEnrichment` (Pydantic) mirrored 1:1 in `frontend/lib/polyglot-api.ts` (`LemmaEnrichment` interface) — drift is caught by `frontend/lib/__tests__/polyglot-enrichment-shape.test.ts` which loads a real LLM-emitted fixture and asserts the TS interface accepts it. Glossless / variant / function-word / proper-name lemmas are skipped. **Selection policy (2026-05-21)**: `find_unenriched_lemmas` prioritises by review proximity in three buckets — `acquiring` (sorted by `acquisition_next_due` ASC, so the next-to-be-reviewed lemma's lookup card lands first) → `learning`+`lapsed` (by `frequency_rank` ASC) → `encountered` (frequency fallback). **`known` lemmas are excluded** — once graduated, the lookup card stops being load-bearing for memory-hook reinforcement. Diverges from Alif's `backfill_root_enrichment.py` / `backfill_pattern_enrichment.py` which include `known` because Arabic FSRS cards keep cycling through long stability windows. `find_unenriched_lemmas(include_failed=True)` re-picks `failed`, `done_flagged` (legacy), AND `done_partial` for cleanup runs and prompt-improvement reruns. |
| Modern Editorial design tokens | **Polyglot-original (2026-05-21)** | `frontend/lib/polyglot-design-colors.ts` + `frontend/lib/polyglot-design-tokens.ts` — single source for the palette, era colors (Mycenaean / Homeric / Classical / Koine / Byzantine / Modern), type scale, spacing, radius. **Why split**: design-colors.ts has no React-Native imports so Jest (ts-jest, node env) can read the era-color map without pulling RN. design-tokens.ts re-exports + adds Platform-dependent font fallbacks. Used by `polyglot.tsx` (reader), `polyglot-review.tsx` (review screen), `polyglot-lookup-card.tsx` (shared middle-density card), `polyglot-lemma/[id].tsx` (full detail). |
| Lookup card (mid-density tap surface) | **Polyglot-original (2026-05-21)** | `frontend/lib/polyglot-lookup-card.tsx` — middle density between the reader's one-line gloss bar and the full lemma detail page. Shows head row (lemma + gloss + POS + freq + ancient cognate chip), compact diachrony peek ("Classical X → Modern Y"), one literary quote with era-color pill, top 2-3 collocations, false-friend warning, "View full philology ›" link to `/polyglot-lemma/{id}`. Used by `polyglot.tsx` (reader tap state) AND `polyglot-review.tsx` (word-tap state). Both call sites lazy-fetch `LemmaDetail` via `getLemmaDetail(id)` with `detailRequestRef` guarding stale responses; the card renders the head row immediately from already-loaded TokenView/WordRender/IntroCard data and streams enrichment in async. |
| Lemma detail screen | **Polyglot-original (2026-05-21)** | `frontend/app/polyglot-lemma/[id].tsx` — full Modern Editorial detail. Vertical color-coded diachrony timeline, cognates table with EN/LA/DE language pills, literary quotes with era-color left rule, register section with collocations + false-friend warning. Empty states: header renders even when `enrichment=null` (shows "No philology yet — appears after next enrichment pass"). Route registered in `_layout.tsx` as `polyglot-lemma/[id]` with `href: null` (hidden from tab bar). Manifest test `frontend/lib/__tests__/language-context.test.ts` asserts `/polyglot-lemma/123` classifies as `el`. **Back navigation** (PR #118): reachable from the reader, the intro card, and the sentence-review gloss card; centralised on a single `goBack()` helper using `router.canGoBack()` with a `/polyglot` fallback so back returns the learner to wherever they came from (previously the back affordance was labelled `‹ Reader`, which was misleading for the review-side entry points). Mirrors Alif's `word/[id].tsx` pattern. **Return-to-position + swipe-to-go-back (2026-05-22):** PR #118's `goBack()` was necessary but not sufficient — because every screen is a flat sibling in one `<Tabs>` navigator (there is no `<Stack>` anywhere), pushing into this detail screen tears the reader down, so on return `router.back()` rebuilt `polyglot.tsx` from scratch and dropped the learner on the story's first content page with scroll/selection lost. Fixed in `polyglot.tsx` by persisting a **reading cursor** (story + page + scroll offset + open lookup-card word) to AsyncStorage (`@polyglot:readingCursor`, 15-minute recency gate so a genuine cold start still opens on the story list) and restoring it on remount. The reader writes the cursor on every cursor-state change, and again with the freshest scroll offset right before `router.push` into the detail screen, and clears it when the learner explicitly taps "‹ Library". This mirrors Alif's AsyncStorage-survives-remount approach (`index.tsx` session cache), **not** a `<Stack>` migration. The detail screen itself also gains a **swipe-right-to-go-back** gesture via `PanResponder` (the same horizontal-pan pattern as `WordInfoCard` — no `react-native-gesture-handler` dep), since a Tabs route has no native edge-swipe; only a clearly-horizontal rightward drag is captured so vertical scrolling of the philology body still works. `translateX` resets to 0 after a committed back (for the tab-reuse case where the lemma effect won't refire) and on gesture terminate (so an interrupted drag springs back instead of stranding the screen offset). **Back-nav root cause + real fix (PR #130, 2026-05-22):** #118/#123/#124/#125 preserved *state* across the round-trip but never fixed where back actually *lands* — pressing Back / swiping from philology still didn't return to Review. Root cause: the shared `<Tabs>` used the bottom-tab default `backBehavior="firstRoute"`, so `router.back()` resolved to the *first* tab (`index` = Arabic Reading), then the language-sync `router.replace` bounced to `/polyglot` (Greek Reading). The detail screen's `goBack()` was always correct; the navigator's interpretation of "back" was wrong. Fixed by setting `backBehavior="history"` on `<Tabs>` in `_layout.tsx` (returns to the previously focused tab). One prop; the snapshot + cursor + swipe machinery above is kept (it preserves state if the tab is remounted on return). |
| Picker generated-first preference + repeat control | **Polyglot-original (2026-05-23)** | `sentence_selector.py` — at review time, generated (LLM) sentences are preferred over the textbook page-of-record. Source is the primary sort tier: `GENERATED_SOURCES={"llm"}` strictly outranks textbook material. Within a tier, `score = (0.3 + 0.7·comprehensibility) · page_cooldown · sentence_recency`; textbook sentences from pages viewed within `PAGE_COOLDOWN_DAYS=7` get `RECENT_PAGE_PENALTY=0.2`, and any sentence shown within `SENTENCE_RECENCY_HOURS=24` gets `RECENT_SENTENCE_PENALTY=0.25`. The repeat penalty is a soft fallback, not a veto, so one-candidate words still have material but a recently shown all-known scaffold stops dominating fresh alternatives. Picker refuses non-content target lemmas at entry and logs candidate counts/reasons for analysis. |

## Development workflow

```bash
# From polyglot/
.venv/bin/uvicorn app.main:app --port 3001              # run dev server
POLYGLOT_QUALITY_GATE=1 .venv/bin/uvicorn app.main:app --port 3001  # with quality gate
.venv/bin/python -m pytest                              # tests (fast)
.venv/bin/python -m pytest -m slow                      # tests requiring real NLP models
```

## Deployment

Polyglot deploys from Git `main`; do not `scp` changed files or cron wrappers
to production. From local `main`, after tests pass:

```bash
polyglot/deploy/deploy-polyglot.sh
```

The script pushes `main` if needed, pulls `origin/main` in `/opt/alif`, links
`/opt/polyglot-update-material.sh` to the repo copy under
`/opt/alif/polyglot/deploy/`, reinstalls `polyglot-backend`, restarts
`polyglot-backend`, and health-checks port `3002`.

## Code style — same as Alif

- Python 3.11+, type hints, pydantic for API schemas, SQLAlchemy 2.x.
- No comments unless WHY is non-obvious (same rule as the root CLAUDE.md).
- Branch prefix `sh/` for any feature branches.
- No emoji unless explicitly requested.

## Phase-2 extraction commitment

The README's "fork-then-converge" plan says: after ~6 weeks of dogfooding polyglot alongside Alif, we extract a shared `alif_core/` package (FSRS, acquisition, session builder, ULK lifecycle). Both backends import from it. Don't extract before — abstracting from one working example is the premature-abstraction trap. **When the time comes**, this CLAUDE.md should be updated to point at `alif_core/` for shared logic.
